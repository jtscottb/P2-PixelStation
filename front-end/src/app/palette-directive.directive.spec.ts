import { PaletteDirectiveDirective } from './palette-directive.directive';

describe('PaletteDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PaletteDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
